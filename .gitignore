# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Static-site build output (when /docs/ has a build pipeline)
docs/public/
docs/resources/
docs/_site/
docs/.hugo_build.lock

# Python (in case a future skill ships Python helpers)
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.env

# Local user data — sentinel files that should NEVER be committed even if
# someone accidentally puts them in the repo. Defense in depth.
holdings.md
lifetime-earnings.md
transactions/
fu-money-log/
*-private.md
